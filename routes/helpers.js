// routes/helpers.js
const fs = require("fs");
const path = require("path");

const parseWhatsAppChat = (chatData) => {
  // Implement parsing logic if needed
  // For now, we assume the chat data is already in the expected format
  return chatData;
};

const parseInstagramChat = (chatData) => {
  // Implement parsing logic if needed
  // For now, we assume the chat data is already in the expected format
  return chatData;
};

module.exports = {
  parseWhatsAppChat,
  parseInstagramChat,
};
